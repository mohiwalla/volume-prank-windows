# Instructions to build binary from source code

run command in terminal in the root of directory.

```bash
x86_64-w64-mingw32-g++ main.cc -o main.exe -lwinmm -static
```

Run the binary and enjoy..

~ mohiwalla
